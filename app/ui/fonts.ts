import localFont from "next/font/local";

export const patrickHand = localFont({
  src: "../fonts/PatrickHand-Regular.ttf",
  variable: "--font-patrick-hand",
  display: "swap",
});

export const rogueScript = localFont({
  src: "../fonts/RougeScript-Regular.ttf",
  variable: "--font-rouge-script",
  display: "swap",
});
